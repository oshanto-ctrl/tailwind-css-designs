{
  "devDependencies": {
    "tailwindcss": "^3.4.5"
  },
  "scripts": {
    "watch": "npx tailwind -i ./src/global.css -o ./public/styles.css --watch",
    "serve": "http-server./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
